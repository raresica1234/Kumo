spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kumo
    username: postgres
    password: postgres
  jpa:
    hibernate.ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

redis:
  host: localhost

springdoc:
  swagger-ui:
    path: /swagger-ui.html
#  paths-to-match: /api/**

kumo:
  owner:
    username: raresica1234

spring.data.redis.repositories.enabled: false # Disables autoscanning of repositories